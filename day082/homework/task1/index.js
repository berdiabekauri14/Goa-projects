// Create an array of numbers and use `filter()` to return only the prime numbers.

const numbers = [1, 2, 3, 4, 5];

const filTer = numbers.filter(numbers, numbers / numbers / 1);

console.log(filTer)